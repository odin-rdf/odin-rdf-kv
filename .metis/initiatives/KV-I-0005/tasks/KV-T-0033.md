---
id: measure-the-sweep-admit-it-to-the
level: task
title: "Measure the sweep, admit it to the suite, and document step 7"
short_code: "KV-T-0033"
created_at: 2026-09-25T09:14:06.553087+00:00
updated_at: 2026-09-25T09:14:06.553087+00:00
parent: KV-I-0005
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
initiative_id: KV-I-0005
---

# Measure the sweep, admit it to the suite, and document step 7

## Parent Initiative

[[KV-I-0005]]

## Objective

Close the initiative: measure what the sweep costs, decide what runs by default and in CI (KV-I-0005 D9), and bring the documents up to date.

## Acceptance Criteria

- [ ] Measured on macOS arm64 and Linux arm64: the default sweep's time (kill and power-loss, workloads 1–5) in the hooked `-o:speed` build with `KV_NO_SYNC`, and the `KV_CRASH` sweep's. If the default is over about 2 s, shrink it or move part of it behind `KV_CRASH`, and record which.
- [ ] The CI invocation D9 describes is written down in the initiative's Results, ready for the workflow: `odin test tests -o:speed -define:KV_IO_HOOK=true -define:KV_NO_SYNC=true`, and the cross-target checks, measured for time.
- [ ] `CLAUDE.md`: build-and-test lines for `KV_IO_HOOK`, `KV_NO_SYNC`, `KV_CRASH`, `KV_CRASH_SUBSETS`, `KV_FUZZ*` and `KV_KILL`; the code map and test-helper list for `tests/crash.odin` and `tests/killer/`; the invariant that all file I/O goes through the three hooked procedures; `.Poisoned` and the new-file rule; the "Next up" line.
- [ ] The vision's current state and step 7 marked done, with the success criteria's evidence.
- [ ] The initiative's **Results**: exit-criteria evidence, deviations, known limitations, measurements.
- [ ] `scripts/test.sh`, `scripts/test.sh --steady` once, and `scripts/test-linux.sh` (arm64, amd64) pass.

## Implementation Notes

### Dependencies
All other tasks of KV-I-0005.

## Status Updates

*None yet.*
